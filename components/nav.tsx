import { SafeAreaView, Text, View } from "react-native";

export const Nav = () => {
    return (
        <SafeAreaView className="absolute top-0 left-0 w-screen bg-black p-3">
            <Text className="text-white text-lg">This is a test</Text>
        </SafeAreaView>
    );
};
