import { Text, View } from "react-native";

export const Nav = () => {
    return (
        <View className={`absolute top-0 left-0 w-screen bg-zinc-800 p-3`}>
            <Text className={`text-stone-50 text-lg`}>This is a test</Text>
        </View>
    );
};
