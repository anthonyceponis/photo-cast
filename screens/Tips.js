import { Text, View } from "react-native";
import React from "react";

export function TipsScreen() {
    return (
        <View style={{flex: 1, alignItems: "center", justifyContent: "center"}}>
            <Text>This is the Tips Screen </Text>
        </View>
    );
};